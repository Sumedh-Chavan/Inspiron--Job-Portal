<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- ✅ Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Csiapp_2"
        tools:targetApi="31">
        <activity
            android:name=".MyFirebaseMessagingService"
            android:exported="false" />
        <!-- ✅ Set LoginActivity as Launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ Main Activity (After Login) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Csiapp_2" />

        <!-- ✅ Add SeekerDashboardActivity -->
        <activity
            android:name=".seeker.SeekerDashboardActivity"
            android:exported="true"
            android:label="Dashboard"
            android:theme="@style/Theme.Csiapp_2">
        </activity>

        <activity android:name=".seeker.JobListActivity"/>

        <activity android:name=".seeker.JobDetailsActivity"/>

        <activity android:name=".seeker.ApplyJobActivity"/>

        <activity android:name=".seeker.SeekerProfileActivity" />

        <activity android:name=".seeker.SeekerInboxActivity" />


        <!-- ✅ Firebase Cloud Messaging (FCM) Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service> <!-- ✅ Firestore Metadata -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel" />
    </application>

</manifest>